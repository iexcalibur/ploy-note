/**
 * Cloud has been removed — local-first only.
 * This component is kept as a no-op stub since 6 files import it.
 */
export const EnableCloudPanel = (_props: { onCloseSetting?: () => void }) => {
  return null;
};
